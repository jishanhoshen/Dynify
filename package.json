{
  "name": "dynify",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/js/app.js --dist-dir public/dist && parcel build src/css/style.css --dist-dir public/dist",
    "watch": "parcel watch src/js/app.js --dist-dir public/dist && parcel watch src/css/style.css --dist-dir public/dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
